{
    "version": 4,
    "configurePresets": [
        {
            "name": "nix",
            "generator": "Unix Makefiles",
            "binaryDir": "${sourceDir}/Build/Nix-Make-Debug"
        }
    ],
    "buildPresets": [
        {
            "name": "nix",
            "configurePreset": "nix"
        }
    ]
}